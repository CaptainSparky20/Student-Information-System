site admin username = cyborg@gmail.com
site admin password = password123

hafiz@gmail.com
hafizcqbreyer123

############################


Account creation test

from accounts.models import CustomUser

# Create a student user
student = CustomUser.objects.create_user(username='alesya', password='password123', role=CustomUser.Role.STUDENT)

# Create a lecturer user
lecturer = CustomUser.objects.create_user(username='hafiz', password='password123', role=CustomUser.Role.LECTURER)

# Create an admin user
admin = CustomUser.objects.create_user(username='zirah', password='password123', role=CustomUser.Role.ADMIN)


ui
Reference Note: Glassmorphism/Blur Styles Used
For cards and topbar:

bg-white/10 or bg-white/5

dark:bg-white/5 or dark:bg-white/10

border border-white/20 or dark:border-white/10

backdrop-blur-md (or use backdrop-blur-xl for heavier blur)

rounded-2xl for large rounded corners

shadow-xl for prominent shadow

For profile dropdown:

bg-white/40 dark:bg-white/10 (translucent, glass effect)

border border-white/20 dark:border-white/10

backdrop-blur-md

rounded-2xl

shadow-2xl

General pattern:

bg-white/opacity dark:bg-white/opacity border border-white/opacity dark:border-white/opacity backdrop-blur-md rounded-2xl shadow-xl


Reference: 
bg-white/10 dark:bg-white/5 backdrop-blur-md rounded-2xl border border-white/20 dark:border-white/10 shadow-xl transition-all


